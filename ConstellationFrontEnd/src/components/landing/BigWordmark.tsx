const BigWordmark = () => (
  <div className="font-display font-semibold leading-[0.85] tracking-[-0.05em] text-white-soft">
    <div style={{ fontSize: "clamp(2.5rem, 7vw, 6.5rem)" }}>TEAM USA</div>
    <div className="text-gradient-usa" style={{ fontSize: "clamp(2.5rem, 7vw, 6.5rem)" }}>
      CONSTELLATION
    </div>
  </div>
);
export default BigWordmark;
